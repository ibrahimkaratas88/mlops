version: "3.7"

services:
  app-server:
    image: 830500671980.dkr.ecr.us-east-1.amazonaws.com/mlops:latest
    deploy:
      mode: global
      update_config:
        parallelism: 2
        delay: 5s
        order: start-first
    ports:
      - "8501:8501"
    networks:
      - maclen

networks:
  maclen:
    driver: overlay